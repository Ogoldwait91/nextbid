class JssSpec {
  static const int maxGroups = 15;
  static const int maxRows = 40;

  // Bank protection rule (from docs)
  static const String bankProtectionPrefix = "AWARD WORK CONTAINED WITHIN";
  static const String bankProtectionPool = "L--";
}
