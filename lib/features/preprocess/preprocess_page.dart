import 'package:flutter/material.dart';

class PreProcessPage extends StatelessWidget {
  const PreProcessPage({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Pre-Process'));
}
