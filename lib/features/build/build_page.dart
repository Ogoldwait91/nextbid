import 'package:flutter/material.dart';

class BuildBidPage extends StatelessWidget {
  const BuildBidPage({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Build'));
}
